services:
  metube:
    build: .
    container_name: metube
    restart: unless-stopped
    ports:
      - "80:8081"
    volumes:
      - /mnt/omv_media/YouTube_download:/downloads
      - /mnt/omv_media/Music/YouTubeDownloads:/audio_download
